[build]
  command = "npm run build"
  publish = "dist"

# Alleen routes die als statische HTML bestaan worden geserveerd.
# Willekeurige (spam-)paden krijgen een echte 404, zodat GA4 geen junk registreert.
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
