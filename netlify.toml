[build]
  command = "npm run build"
  publish = "dist"

# SPA-fallback: onbekende paden laden de app; React Router toont daar de NotFound-pagina.
# GA4-pageviews worden bij NotFound NIET gestuurd (zie App.tsx), zodat spam-paden niet meetellen.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
